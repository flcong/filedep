from setuptools import setup

setup(
      name='filedep',
      version='0.1',
      description='filedep: A small python tool to check file dependency',
      author='Francis Cong',
      url=''
      license='MIT',
      packages=['filedep'],
      zip_safe=False
)